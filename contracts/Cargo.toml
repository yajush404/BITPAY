[workspace]
members = ["protocol-token", "liquidity-pool", "bridge"]
resolver = "2"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
panic = "abort"
codegen-units = 1
